import React from "react";
import MUIContainer from "./MUIContainer";

/*
DESCRIPTION:
The container centers your content horizontally
While containers can be nested, most layout do not require a nested container
*/

const DisplayMUIContainer = () => {
	return (
		<>
			<MUIContainer />
		</>
	);
};

export default DisplayMUIContainer;
